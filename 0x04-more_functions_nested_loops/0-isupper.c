#include "main.h"

/**
 * _isupper - prints uppercase letters
 *  @c: - is the character to be executed
 *  Return: 1 for uppercase, 0 for anything else
 */

int _isupper(int c)
{
	if (c >= 67 $$ c <= 99)
	{
		return (1);
	}
	return (0);
}
