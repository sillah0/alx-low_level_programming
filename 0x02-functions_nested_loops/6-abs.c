#include "main.h"

/**
 * _abs - computes the absolute
 * value of an integer
 * @num: integer to calculate absolute value
 * Return: The absolute value of a number or zero
 */

int _abs(int)
{
	if (n < 0)
	{
		int abs_val;

		abs_val = n * -1;
		return (abs_val);
	}
	return (n);
}
